services:
  api-docs:
    image: docker.swagger.io/swaggerapi/swagger-ui:latest
    ports:
      - "127.0.0.1:8081:8080"
    environment:
      SWAGGER_JSON: /docs/openapi.yaml
    volumes:
      - type: bind
        source: ./yaml
        target: /docs
        read_only: true
